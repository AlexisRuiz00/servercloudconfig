server:
  port: 8081

spring:
  data:
    mongodb:
      host: 34.205.100.14	
      port: 27017

eureka:
  client:
    healthcheck:
      enabled: true
    serviceUrl:
      defaultZone: http://172.31.70.152/eureka/
  instance:
    preferIpAddress: true
    metadataMap:
      instanceId: ${vcap.application.instance_id:${spring.application.name}:${spring.application.instance_id:${random.value}}}
