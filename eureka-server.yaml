server:
  port: 8761

eureka:
  instance:
    hostname: ec2-34-205-100-14.compute-1.amazonaws.com
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/
  server:
    enableSelfPreservation: false
