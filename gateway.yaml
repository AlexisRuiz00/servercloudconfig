
server:
  port: 8085 #the default port used for zuul service
  
zuul:
  ignoredServices: '*'
  routes:
    activity:
      path: /activity/**
      serviceId: activity
    login:
      path: /login/**
      serviceId: login
     
